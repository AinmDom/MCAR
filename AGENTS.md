# MCAR 多智能体协作约定

本文件适用于仓库根目录及其全部子目录。Codex、DeepSeek 以及其他参与本项目的
智能体都必须遵守以下协作约定。

## 开始工作前

1. 阅读 `docs/EXPERIMENT_LOG.md` 顶部最近三条记录（时间倒序），确认当前进度、
   最新结论、运行中任务与阻塞项；再检查 `git status --short` 和当前分支/HEAD。
2. 科研事实以实际配置、产物和 `docs/EXPERIMENT_LOG.md` 为准，不能只凭摘要推断。
3. 若日志表明已有 run 正在运行或状态不明，不得启动与其范围重叠的训练、评价、
   汇总或提交；可以做不冲突的只读检查，并把发现写入实验日志供后续接手。

## 目标/任务模式中的训练启动

启动目标（goal）或任务（task）模式时，“启动训练”应作为任务列表的最后一个任务，训练启动以后则退出任务模式，完成本轮对话。

## 记录规则

每完成一个可验证步骤，就在回复用户前于 `docs/EXPERIMENT_LOG.md` 顶部新增一条
记录（时间倒序，反映当前最新状态，无需另设交接页）。可验证步骤包括：

- 协议、配置或数据边界冻结；
- 正式 run 启动；
- run 成功、失败、中止或被判定需要重测；
- 汇总、完整性检查或候选排序完成；
- 实验决策产生；
- 新阻塞出现或已有阻塞解除。

仅浏览文件、讨论方案或做无结论的临时检查，不必制造日志噪声。

记录要求：

1. 长任务启动时写清范围、PID/进程、输出路径和检查方式；任务结束或安全停止后再
   补一条收尾记录。不确定进程是否仍在运行时，不得把状态写成已完成。
2. 每条记录必须包含带时区的时间、agent、状态、动作、证据路径、关键结果或故障、
   完整性信息、下一步和阻塞项。数值结论必须写精确值与口径。历史记录只追加、不
   重写、不删除。
3. 声称“完成”前必须检查实际产物。只有配置、命令或空目录时只能写“已准备”或
   “已启动”。训练/评价条目应明确 `test_subjects_read`、finite/完整性、best/末点
   预算判据和 Git 状态；不适用的字段写 `N/A`。
4. 证据使用仓库相对路径，尽量链接到已跟踪的小型 JSON/CSV/Markdown；大型本地
   artifact 同时写生成目录和复现命令或配置。
5. 编辑文件前重新读取 `docs/EXPERIMENT_LOG.md` 顶部和 `git status --short`。若
   发现陌生的新条目或未提交修改，保留它们并在最新状态上增量更新，避免用旧缓冲区
   覆盖文件。

## 数据与实验边界

- 严格遵守各阶段的预注册协议、split 和 test 访问规则。本约定不授予新的 test
  使用权限，也不允许根据 test 结果调参。
- 不把未提交修改、未知来源进程或缺少 provenance 的 artifact 当作正式证据。
- 不自行清理另一智能体的进程、checkpoint、日志或工作区修改；需要接管时先在
  `docs/EXPERIMENT_LOG.md` 顶部留下明确的释放或中止记录。

## 论文写作工作区

- 作者于2026-09-04指定 `paper write/` 为后续论文写作的唯一工作目录。
- 当前总框架：`paper write/PAPER_OUTLINE_ARS.md`。作者于2026-09-10指定后续正式论文工程为 `paper write/grand_paper/`，LaTeX入口为 `paper write/grand_paper/main.tex`；其他历史稿仅作对照，不再作为正式写作入口。
- `paper write/` 其余内容默认不纳入版本库；`grand_paper/` 是唯一例外，并由该目录内的 `.gitignore` 继续排除 LaTeX 可再生编译缓存和未重新编译的旧 `main.pdf`。
- 新增或修改正文、论文图表、参考文献、写作记录及编译产物均在该目录树内进行，不再向 `reports/` 新建当前稿件文件。
- 作者于2026-09-06指定：智能体修改 `.tex` 文件后，不自动运行 LaTeX 编译、PDF 渲染或版面检查；由作者自行完成编译与检查。只有作者在后续请求中明确要求时，才执行这些操作。
- 实验代码、配置、原始数据及冻结结果保持原位，作为写作的只读证据；旧Bounded历史稿暂留原位，不作为当前写作入口。
- 本文件仍按仓库协作规则维护。历史记录中的旧路径不回写，迁移映射见 `paper write/MIGRATION_CHECK.json`。
