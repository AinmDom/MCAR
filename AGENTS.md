# MCAR 多智能体协作约定

本文件适用于仓库根目录及其全部子目录。Codex、DeepSeek 以及其他参与本项目的
智能体都必须遵守以下交接流程。

## 开始工作前

1. 完整阅读 `docs/AI_HANDOFF.md`，再检查 `git status --short` 和当前分支/HEAD。
2. 阅读交接页中链接的协议、结果和日志；科研事实以实际配置、产物和
   `docs/EXPERIMENT_LOG.md` 为准，不能只凭交接摘要推断。
3. 如果交接内容与仓库证据冲突，先停止有重叠风险的实验，核实后把差异记录到
   交接页。不要默默覆盖另一方的工作。
4. 若 `Baton` 不是 `FREE`，不得启动与其范围重叠的训练、评价、汇总或提交；可以
   做不冲突的只读检查，并把发现交给当前持棒方。

## 目标/任务模式中的训练启动

启动目标（goal）或任务（task）模式时，对“启动训练”的位置要求按平台区分：

- **DSH**：不一定要把“启动训练”放在最后一个目标/任务。进入任务管理的 pwsh
  进程结束后，其结束消息会通过 tool call 自动唤醒 agent 继续后续目标/任务，
  这是正常且推荐的做法。但训练进行期间，DSH **不得进行主动轮询推理**——不得
  反复调用工具、读取日志或推理来监控训练进度，应保持空闲等待 pwsh 结束消息
  唤醒，避免消耗无意义的 token。
- **Codex**：没有类似的自动唤醒机制，因此“启动训练”应放在列表的最后一个
  目标/任务；若无法放在最后，也不得持续监控训练，否则会持续消耗额度。

训练启动后，agent 即结束本轮监控动作，等待训练进程自行结束；训练结束后的后续
状态通过交接机制（`docs/AI_HANDOFF.md`）接管。

## 哪些时点必须交接

每完成一个可验证步骤，就在回复用户前更新 `docs/AI_HANDOFF.md`。可验证步骤包括：

- 协议、配置或数据边界冻结；
- 正式 run 启动；
- run 成功、失败、中止或被判定需要重测；
- 汇总、完整性检查或候选排序完成；
- 实验决策产生；
- 新阻塞出现或已有阻塞解除。

仅浏览文件、讨论方案或做无结论的临时检查，不必制造日志噪声。

## 更新规则

1. 长任务启动后把 `Baton` 改为 `CODEX` 或 `DEEPSEEK`，写清范围、PID/进程、输出
   路径和检查方式；任务结束或安全停止后改回 `FREE`。不确定进程是否仍在运行时，
   不得写 `FREE`。
2. 更新顶部“当前接力状态”，并在底部“追加式交接记录”增加一条记录。历史记录
   只追加、不重写、不删除。
3. 每条记录必须包含带时区的时间、agent、状态、动作、证据路径、关键结果或故障、
   完整性信息、下一步和阻塞项。数值结论必须写精确值与口径。
4. 正式实验结论还必须在 `docs/EXPERIMENT_LOG.md` 顶部增加详细条目；实时交接页只
   保留接手所需摘要，不能替代实验日志。
5. 声称“完成”前必须检查实际产物。只有配置、命令或空目录时只能写“已准备”或
   “已启动”。训练/评价条目应明确 `test_subjects_read`、finite/完整性、best/末点
   预算判据和 Git 状态；不适用的字段写 `N/A`。
6. 证据使用仓库相对路径，尽量链接到已跟踪的小型 JSON/CSV/Markdown；大型本地
   artifact 同时写生成目录和复现命令或配置。
7. 编辑前立刻重新读取交接页和 `git status --short`。若发现陌生的新条目或未提交
   修改，保留它们并在最新状态上增量更新，避免用旧缓冲区覆盖文件。

## 数据与实验边界

- 严格遵守各阶段的预注册协议、split 和 test 访问规则。交接机制不授予新的 test
  使用权限，也不允许根据 test 结果调参。
- 不把未提交修改、未知来源进程或缺少 provenance 的 artifact 当作正式证据。
- 不自行清理另一智能体的进程、checkpoint、日志或工作区修改；需要接管时先在交接
  页留下明确的释放或中止记录。

