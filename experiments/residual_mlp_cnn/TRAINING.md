# MLP + CNN v3 / v3.1 训练产物

checkpoint、W&B 缓存和本机日志位于 `artifacts/training/`。审核后的训练历史、
validation/test 指标位于
`results/residual_mlp_cnn/mlp_cnn_n03_v3/training/`。

v3.1 本机 checkpoint、W&B 缓存与日志位于
`artifacts/training/mlp_cnn_n03_v31_finetune_v3_strict_ild_wandb_online/`；
精选的配置、训练历史、validation/test 指标位于
`results/residual_mlp_cnn/mlp_cnn_n03_v31/training/`。模型权重仍只保留在
被 Git 忽略的 `artifacts/` 中。

正式 W&B run：
`https://wandb.ai/luyoung/mcar-mlp-cnn-v31/runs/yhl3z70n`。
