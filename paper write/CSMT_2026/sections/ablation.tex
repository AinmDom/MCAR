% Author-requested separation; no external-baseline or single-factor claim.
\FloatBarrier
\subsection{消融与内部变体分析}
\label{sec:ablation}
本节区分验证集上的组件比较与历史测试队列上的项目内部变体比较。后者仅用于展示开发路线的差异，不作为外部横向基线；两类比较均不构成严格的单因素消融，不能将差异直接归因于某一个模块。

% Role: component evidence separated from test and isolated ablation. Evidence: E4a/E4p.
\subsubsection{频谱细化器的验证集组件比较}
在同一44名验证被试及767方向上，Hybrid相对FiLM E130集成的三项主要频谱误差均降低（表\ref{tab:component-results}）。全域ERB、对侧$25^\circ$ ERB和对侧高频分别降低0.023122、0.016183和0.200758\,dB，配对区间均低于零，对应改善人数为44、43和44人。水平面ILD差为$-0.000787$\,dB，区间跨零。这与追加频谱细化后的谱误差改善一致，但不说明已消除测试集中的ILD代价。

\input{tables/component_results}

两侧都是三成员集成，差异不仅包含新增CNN结构，还包含该模块的额外训练与复合目标优化。该比较属于开发证据，不是对CNN结构、各损失项或集成效应的逐项因果隔离，也不与测试集均值混合报告。

\subsubsection{项目中间模型的补充比较}
MCAR v3.5.1和Bounded E25是项目推进过程中的内部中间模型。为保留设计路线及其代价，表\ref{tab:internal-primary}和表\ref{tab:internal-spectral}单列它们与Hybrid的既有结果，使用同一44名历史测试被试及Q26设置，不与前述验证集组件结果混合。它们不是Hybrid在统一预算下逐项移除模块得到的对照，故这些结果只能支持内部变体的描述性比较。

\input{tables/internal_primary_results}
\input{tables/internal_spectral_results}

相对MCAR v3.5.1，Hybrid的优势和代价并存。对侧$25^\circ$ ERB及对侧高频均值差分别为$-0.074093$和$-0.053570$\,dB，95\%区间分别为$[-0.111932,-0.045892]$和$[-0.089022,-0.022387]$\,dB。全域ERB差仅$-0.000393$\,dB且区间含零。与此同时，水平面ILD增加$0.134741$\,dB，区间为$[+0.012705,+0.344849]$\,dB；频谱改善伴随双耳能量关系上的代价。

Hybrid相对Bounded的全域ERB增加$0.023413$\,dB，水平面ILD增加$0.105068$\,dB，两项配对区间均高于零。对侧$25^\circ$ ERB与对侧高频的配对差及95\%区间分别为
\[
\begin{aligned}
\Delta_{\mathrm{Contra25}}&=-0.001121\,\mathrm{dB},&
\mathrm{CI}_{95\%}&=[-0.022054,+0.014415]\,\mathrm{dB},\\
\Delta_{\mathrm{ContraHF}}&=-0.021669\,\mathrm{dB},&
\mathrm{CI}_{95\%}&=[-0.050092,-0.000766]\,\mathrm{dB}.
\end{aligned}
\]
前者区间包含零，后者提示小幅改善，不能概括为全部频谱指标均优于该内部变体。

全域ERB与频带ILD在内部比较中仍以Bounded的均值更低。Hybrid的频带ILD为1.6205\,dB，Bounded为1.5734\,dB；Hybrid的全域ERB和频带ILD相对Bounded分别高2.95\%和3.00\%。因此，外部主表中的最低均值位置不能推广为对全部内部设计的支配，也不能用1\%容差将这两项合并为内部最佳。频带ILD缺少直接Hybrid中心的补充端点配对区间，这里不作显著性判断。

相对MCAR，Hybrid在全域ERB及对侧$25^\circ$ ERB均有39/44人改善，但前者总体配对区间仍含零；水平面ILD仅20/44人改善。测试被试P0339的MCAR水平面ILD为1.8838\,dB，低于Hybrid的6.0497\,dB，进一步表明内部变体间的收益具有指标和个体依赖性。
\FloatBarrier
