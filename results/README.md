# 精选结果

这里仅保存经过口径审核、需要用于报告、论文或项目交接的轻量结果。

- `baselines/mca/`：SH、SUpDEq + SH 和 MCA 基线汇总；
- `residual_mlp/`：MLP v1/v2 的训练指标与严格重建结果；
- `residual_mlp_cnn/`：v3 的训练指标、严格重建表格和精选图片。

checkpoint、HDF5、MAT、日志及未筛选图片应留在 `artifacts/`。
